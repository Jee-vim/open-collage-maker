# Base URL of the backend API (the Render/Railway service).
# Leave empty to use same-origin (/api), e.g. when proxied locally.
# On Vercel, set this in the project's Environment Variables instead.
VITE_API_URL=
